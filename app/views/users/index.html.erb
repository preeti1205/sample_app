<% provide(:title, 'All users') %>
<h1>All users</h1>


<%= will_paginate %>

<!-- <ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= render user%>
    </li>
  <% end %>
</ul> -->

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
